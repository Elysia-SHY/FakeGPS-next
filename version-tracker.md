# Version Tracker - FakeGPS-next

> **当前真实基准版本**：`2.9.0`  
> **Android 内部版本**：`versionCode = 209`  
> **Android 显示版本**：`versionName = "v2.9"`  
> **交付存放目录**：`d:\Desktop\fake gps\`  
> **最新安装包路径**：`d:\Desktop\fake gps\FakeGPS-v2.9-LATEST.apk`  
> **构建时间**：2026-09-09 14:18  

---

## 版本演进与变更日志 (Version History)

### [2.9.0] - 2026-09-09
- **构建状态**：非 Root 环境安全隐藏高阶拟真功能，悬浮摇杆下拉控制面板深度重构为 iOS 极简拟物卡片，GitHub 官方文案全量规范与发布规整 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 209`, `versionName = "v2.9"`
- **交付与目录规整**：
  1. 所有构建产物统一置于 `d:\Desktop\fake gps\` 文件夹；
  2. 交付产物：`FakeGPS-v2.9-LATEST.apk`、`FakeGPS-v2.9.apk`，彻底清理过时旧版安装包，仅保留当前版本与原始黄金参考基准；
  3. GitHub 仓库同步最新源码与 Release。
- **核心功能与交互演进**：
  1. **ROOT 权限安全自适应隐藏 (Root Feature Auto-Hiding)**：
     - 在「路线模拟」页中，「运动步频与计步仿真」与「GPS 底层拟真与抗检测」严格受 `isRootActive` 状态守卫保护；在非 ROOT 手机或未授权环境下，此两项高阶功能**自动完全隐藏**，界面回归极致清爽简洁，有 ROOT 环境时自动高亮展示；
     - 明确软件三种梯级工作模式：免 ROOT 模式（开发者选项模拟位置开箱即用）、ROOT 模式（解锁传感器拟真）、LSPosed 模块（可选的系统级增强，实现 0 闪回、0 注入、抹除 Mock 标志）。
  2. **全局悬浮摇杆下拉控制面板重构 (iOS Translucent Options Card)**：
     - 彻底重构原先青绿色（Teal）老旧简陋下拉面板；
     - 切换胶囊重构为精致的 iOS 拟物圆角胶囊 `[ ⚙️ 控制 ▾ ]` / `[ ⚙️ 收起 ▴ ]`；
     - 控制面板重塑为 32px 大圆角磨砂卡片，全面融入 Apple Blue (`#007AFF`)、Apple Green (`#34C759`) 与细红标签 (`#FF3B30`)；
     - 巡航速度、尺寸预设、方向模式与八方向锁定采用 iOS 分段胶囊排布；
     - 非 Root 设备自动剔除「步频仿真」行，体验与主应用一脉相承。

### [2.8.0] - 2026-09-09
- **构建状态**：彻底修复切换步频模式时预估步幅无响应的问题，修复滑动配速条导致累计步数异常上涨的致命逻辑漏洞，计步与步幅进入全动态实时响应时代 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 208`, `versionName = "v2.8"`
- **交付与目录规整**：
  1. 所有构建产物统一置于 `d:\Desktop\fake gps\` 文件夹；
  2. 交付产物：`FakeGPS-v2.8-LATEST.apk`、`FakeGPS-v2.8.apk`，清理过时旧版快捷方式。
- **核心修复与交互重构**：
  1. **彻底消除滑动配速条步数暴涨的逻辑 Bug (Phantom Step Accumulation Fix)**：
     - 原因定位：滑动配速滑块时，每帧触发的高频 `applySpeed(speed)` 中错误调用了 `sensorEngine.updateTick(speed, 1f)`，导致每次微调配速都向累计步数强行增加步数；
     - 修复方案：完全移除配速滑动对 `updateTick` 的误调，步数仅在路线真正模拟运行（Service 后台驱动）时按物理周期平稳增长；
     - 引入小数级步数累加器 (`fractionalSteps`)，精准统计毫秒/小周期步频步数，杜绝任何提前强制舍入导致的步数虚高。
  2. **切换步频模式与配速时预估步幅实时响应 (Real-time Dynamic Stride & Cadence)**：
     - 原因定位：原先预估步幅只在运行中的固定 tick 回调中更新，未运行时步频固定显示 `-- SPM`、步幅固定写死为 `0.75m`，导致用户点击预设步频（健步 110、慢跑 160、跑马 180、自定义、自适应）时面板毫无反应；
     - 修复方案：
       - 基于运动生物力学公式 `步幅 = 速度 (m/s) / (步频 / 60)`，在 UI 渲染层与引擎层实现全局双向响应；
       - 只要切换步频（如 110、160、180、自定义）或滑动配速条，预估步幅与实时/预估步频看板 0ms 实时联动刷新；
       - 例如 8 km/h 配速下：点击「健步 110」步幅实时变动为 **1.21 m**；点击「慢跑 160」步幅即刻变动为 **0.83 m**；点击「跑马 180」即刻变动为 **0.74 m**；滑动配速至 12 km/h，步幅实时平滑延展至 **1.11 m**，交互体验极其真实丝滑。

### [2.7.0] - 2026-09-09
- **构建状态**：修复「重置位置」被虚假 Mock 缓存劫持的问题，全面重构地图底部 UI 层级布局消除重叠冲突，全量编译验证通过 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 207`, `versionName = "v2.7"`
- **交付与目录规整**：
  1. 所有构建产物统一置于 `d:\Desktop\fake gps\` 文件夹；
  2. 交付产物：`FakeGPS-v2.7-LATEST.apk`、`FakeGPS-v2.7.apk`，清理过时旧版快捷方式。
- **核心修复与交互优化**：
  1. **彻底修复「重置位置」显示虚拟定位的问题 (Strict Real GPS Recovery)**：
     - 原因定位：在开启虚拟定位时，系统 `LocationManager.getLastKnownLocation()` 会被 Mock Test Provider 注入的虚假位置污染，导致复位时读取到的仍然是虚假位置；
     - 深度防御：
       - `CoordinateConverter.isMockLocation` 严格过滤 `loc.isMock` (Android 12+) 及 `loc.isFromMockProvider`，杜绝任何 Mock 假数据；
       - 本地持久化保存通过硬件传感器实际获取的真实硬件 GPS 坐标；
       - 点击「重置位置」时立即调用 `stopPointMock` / `stopSimulation`，强制注销并清理底层 Test Provider (`removeTestProvider`)，确保系统硬件物理定位驱动完全夺回主控权；
       - 主动请求单次鲜活硬件 GPS 定位，并将地图平滑聚焦至 Apple Maps 经典发光蓝环真机位置。
  2. **全面重构地图底部 UI 布局，彻底解决组件重叠与折行遮挡**：
     - **目标点信息卡片结构降维**：将原先 6 个横向排列挤占空间的按钮行重构为清晰的双行卡片布局；第一行展示状态标签、经纬度数值、地理地址与关闭按钮；第二行利用弹性权重 (`Modifier.weight`) 均匀排布“恢复真实 / 开启定位”、“✏️ 划线”、“起”、“终”操作按钮，360~390dp 各种窄屏设备均绝不折行重叠；
     - **自适应悬浮层级高度锚定**：底栏卡片与连续划线状态条统一定位在 `bottom = 86.dp`（高出 70dp iOS 悬浮底栏 16dp）；
     - **右侧悬浮功能钮群 (FABs) 动态避让**：右侧 FAB 升级为统一 46dp 精致圆形玻璃质感悬浮钮（划线模式钮 + 重置真机位置钮）；当底部卡片展开或处于划线模式时，FAB 组自动升至 `bottom = 205.dp`，处于纯净浏览模式时平滑降至 `bottom = 145.dp`，永久保持 25dp 以上安全可视间距，无任何重叠遮挡。

### [2.6.0] - 2026-09-09
- **构建状态**：全面上线地图选点「重置位置」即刻呈现当前真实物理位置功能，打造 Apple Maps 级高亮发光蓝环光标与全自动智能定位协同交互 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 206`, `versionName = "v2.6"`
- **交付与目录规整**：
  1. 所有构建与基准 APK 统一规整至 `d:\Desktop\fake gps\` 文件夹；
  2. 交付产物：`FakeGPS-v2.6-LATEST.apk`、`FakeGPS-v2.6.apk`。
- **地图选点「重置位置」与真实位置呈现核心特性**：
  1. **Apple Maps 级真实物理位置发光光标 (Real Location Puck)**：
     - 在底图图层中常驻绘制 Apple 风格发光蓝环光标（皇家蓝实体核心、纯白外框、半透明发光雷达光晕）；
     - 只要设备硬件 GPS / 网络提供商捕获到真实定位，即可在地图上实时呈现自己的物理实体位置，点选该光标亦可直接查看或设为目标点；
  2. **一键「重置位置」悬浮胶囊按钮**：
     - 地图右下角将单薄图标升级为醒目优雅的 iOS 拟物悬浮胶囊 `[ 🔄 重置位置 ]`；
     - 点击瞬间主动向硬件 GPS 发起即时定位拉取（兼顾缓存 0ms 闪开与硬件单次精确定位），瞬间将地图平滑动画聚焦至真实物理位置；
     - 自动标定当前真实位置为选中目标点，并在底部即刻弹出「🔵 当前真实物理位置」精准卡片，包含经纬度与逆地理编码地址；
  3. **一键恢复真实位置防御**：
     - 若当前已开启虚拟定位且定位在别处，在点击「重置位置」查看真实位置时，底部卡片直接提供「恢复真实」绿色一键按钮，点击即刻停止模拟、恢复硬件真实 GPS，彻底告别来回切换页面的繁琐；
  4. **跨页面联动**：
     - 在「虚拟定位」页点击「重置为真机物理位置」，同样自动停止模拟、复位坐标并自动跳转到「地图选点」页面高亮展示。

### [2.5.0] - 2026-09-09
- **构建状态**：全面加固系统后台保活与防杀机制，完整集成路线模拟中的运动步频与 GPS 底层拟真系统，全量编译验证通过 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 205`, `versionName = "v2.5"`
- **交付与目录规整**：
  1. 所有构建与基准 APK 统一规整至 `d:\Desktop\fake gps\` 文件夹；
  2. 交付产物：`FakeGPS-v2.5-LATEST.apk`、`FakeGPS-v2.5.apk`。
- **后台保活与全链路防杀全面加固**：
  1. **服务粘性自愈与状态持久化**：解决被系统清理重启时因 `intent == null` 导致模拟静默失效的致命问题；经纬度、路线与进度实时保存至本地，重启后自动无缝拉起注入；
  2. **多任务划掉卡片防御 (`onTaskRemoved`)**：任务卡片被划掉时，通过 `KeepAliveHelper` 立即登记 `AlarmManager` 500ms 快速复活哨兵；
  3. **系统级电池优化直达与厂商防杀**：新增 `KeepAliveHelper`，一键弹出原生“允许始终在后台运行”授权框；智能识别小米/红米、华为/荣耀、OPPO/一加、vivo/iQOO、三星等系统，直达“自启动管理”并提供多任务加锁向导；
  4. **长效守护与能耗优化**：单点定点注入调优至 500ms（2Hz，避免触发高频耗电哨兵），WakeLock 延长至 24 小时长效守护；前台通知启用 `FOREGROUND_SERVICE_IMMEDIATE` 与 `CATEGORY_SERVICE`。
- **路线模拟：运动步频与计步器仿真完整上线**：
  1. **实时 4 维步频看板**：在「路线模拟」页新增 Apple 风格拟真看板，实时呈现**实时步频 (SPM)**、**累计步数**、**推算步幅 (m)**与**垂直重力起伏 (m/s²)**；
  2. **5 档步频模式**：支持【智能自适应】（根据配速匹配 95~195 步频）、【健步 110】、【慢跑 160】、【跑马 180】与【自定义精确 SPM】；
  3. **后台与锁屏持续联动**：`MockLocationService` 路线每秒巡航推进中实时注入 `SensorMockEngine`，保障后台锁屏时步数与里程同步持续计步。
- **路线模拟：GPS 底层拟真与抗检测看板上线**：
  1. **多星系统搜星**：`MockLocationEngine` 动态模拟 18~24 颗搜星（北斗/GPS/Galileo 多星融合协议）；
  2. **真实水平精度与微漂移**：动态模拟 ±1.8m ~ ±2.5m 水平精度波动，配合 Box-Muller 高斯微漂移（±0.3m）和自然配速呼吸微抖（±5%），彻底杜绝防作弊算法对静态死点与机械直线的特征检测；
  3. **动态高程与切线航向**：实时计算海拔高度与转弯航向角，提供专业拟真指标概览卡片。

### [2.4.0] - 2026-09-09
- **构建状态**：根治划线模式下左侧出现异常拉伸“长白条”的严重排版 Bug，顺利完成全量构建 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 204`, `versionName = "v2.4"`
- **交付与目录规整**：
  1. 所有构建与基准 APK 统一规整至 `d:\Desktop\fake gps\` 文件夹，桌面根目录保持纯净无散落文件；
  2. 交付包含：`FakeGPS-v2.4-LATEST.apk` (25,989,681 字节)、`FakeGPS-v2.4.apk`、`FakeGPS-debug.apk`。
- **左侧异常拉伸长白条问题深度根治**：
  1. **问题成因诊断**：
     - 当用户进行连点划线时（`drawnWaypoints.isNotEmpty()`），顶部工具栏动态加入了“撤回、清空、保存” 3 枚操作按钮，导致顶部右侧工具栏横向暴增至 8 枚按钮（宽度超 290dp）；
     - 右侧工具栏霸占屏幕全部横向空间，将左侧的“当前定位”地址卡片横向压缩至仅几像素；
     - 由于卡片第一行标题未设置 `maxLines = 1`，Compose 将长达几十字符的文本逐字折行渲染成几十行高，且容器未限制最大高度，最终把整张卡片竖向畸形拉伸成一条贯穿屏幕上下边缘的“长白条”（图钉图标被垂直居中固定在条中央）。
  2. **全面修复方案**：
     - **去除顶部多余重复按钮**：划线状态下，底部划线控制栏已配备专属清晰的【撤销】、【清空】、【保存】按钮，顶部工具栏在划线模式下不再重复添加，维持纯净紧凑的 5 枚核心工具，不再挤压左侧空间；
     - **硬性高度与单行截断防御**：左侧卡片添加 `heightIn(max = 46.dp)`，两行文本均严格锁定 `maxLines = 1` 与 `overflow = TextOverflow.Ellipsis`，从底层杜绝任何情况下容器被竖向撑开成白条的隐患。

### [2.3.0] - 2026-09-09
- **构建状态**：彻底解决首页（地图选点）多处 UI 重叠、碰撞遮挡问题，顺利完成全量构建 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 203`, `versionName = "v2.3"`
- **交付与目录规整**：
  1. 所有构建与基准 APK 统一规整至 `d:\Desktop\fake gps\` 文件夹，桌面根目录保持纯净无散落文件；
  2. 交付包含：`FakeGPS-v2.3-LATEST.apk` (25,989,625 字节)、`FakeGPS-v2.3.apk`、`FakeGPS-debug.apk`。
- **三处 UI 重叠问题彻底根治**：
  1. **右上角工具栏与竖向缩放条重叠修复**：
     - 将 `IosVerticalZoomControl`（`+`/`-` 缩放滑块）的垂直安全上边距从 `56.dp` 调整至 `80.dp`，彻底移至顶部水平工具栏（搜索、划线、道路、底图、GPX）正下方，杜绝底图切换图钉与缩放条的视觉冲突。
  2. **右下角悬浮按钮 (FABs) 与悬浮底栏重叠修复**：
     - 将连续划线 FAB 与物理定位 FAB 的底部间距从 `96.dp` 提升至 `120.dp`（选点展开时提升至 `190.dp`），为悬浮底栏（62dp+安全区）预留出 20dp+ 舒适呼吸间距，杜绝图钉按钮压在底栏和提示卡片上的问题。
  3. **底部提示胶囊/选点卡片与悬浮底栏及 Toast 重叠修复**：
     - 将底部常驻提示胶囊（从 `76.dp` 提至 `114.dp`）、连续划线工具栏（从 `96.dp` 提至 `114.dp`）、选点详情卡片（从 `96.dp` 提至 `114.dp`）以及路线准备胶囊全量向上规整；
     - 将长文本提示精简为极简 iOS 风格：`🎯 轻触底图直接选点定位`，彻底杜绝文字过长横向碰撞右侧 FAB，也避免与 Android 系统 Toast 提示碰撞重叠。

### [2.2.0] - 2026-09-09
- **构建状态**：完成 iOS 悬浮毛玻璃胶囊底栏与巡航配速控制区无包边轻量按键 UI 重构，顺利完成全量构建 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 202`, `versionName = "v2.2"`
- **交付与目录规整**：
  1. 所有构建与基准 APK 统一规整至 `d:\Desktop\fake gps\` 文件夹，桌面根目录保持纯净无散落文件；
  2. 交付包含：`FakeGPS-v2.2-LATEST.apk` (25,989,661 字节)、`FakeGPS-v2.2.apk`、`FakeGPS-debug.apk`。
- **两大 UI 视觉与交互重大升级**：
  1. **iOS 风格悬浮毛玻璃胶囊底栏 (Floating Frosted Glass Capsule TabBar)**：
     - 由传统贴合在屏幕底部的平铺底栏，彻底重构为居中悬浮的 Apple 浮岛毛玻璃胶囊（`RoundedCornerShape(32.dp)`）；
     - 具备多层细腻的环境弥散阴影（`elevation = 16.dp`）与高透微光描边（`0.5.dp, Color.White.copy(0.65f)`）；
     - 选中标签项呈现半透明浅蓝高亮胶囊（`SystemBlue.copy(alpha = 0.12f)`）与弹性微缩放动效；
     - 全局二级页面（地图选点底栏、虚拟定位、路线模拟、路线库）同步适配底部安全内边距（`100.dp` / `168.dp`），彻底杜绝内容与底栏穿透冲突。
  2. **巡航配速控制区域彻底去包边重构**：
     - **彻底去除生硬难看的“一圈包边”**：移除了原分段选择器多层深灰背景框（`TertiarySystemFill`）与多重描边；
     - **横向轻量级 iOS 胶囊按键 (Speed Preset Chips)**：5 枚独立胶囊（`🚶 5`、`🏃 8`、`🚴 20`、`🚗 60`、`🏎️ 100`），选中态为纯净 Apple System Blue 实色填充配纯白加粗文字，未选态为柔和半透明浅层微底，无任何突兀线框；
     - **去包边二次操作按钮**：将原先带有 `.border(...)` 描边的“导入 GPX”与“地图查看”同步改造成无边框 Apple 次级着色填充风格（`SystemBlue.copy(0.12f)`）；
     - **沉浸式数值输入胶囊**：自定义速度输入框改造成与卡片一体化的轻量极简胶囊，配备高亮 `km/h` 徽标。

### [2.1.0] - 2026-09-09
- **构建状态**：完成状态识别真机深度修复、路线划线免遮挡交互优化，顺利完成全量构建 (`outputs/app-debug.apk`)。
- **Android 配置**：`versionCode = 201`, `versionName = "v2.1"`
- **交付与目录归集**：
  1. 所有构建与基准 APK 统一规整至 `d:\Desktop\fake gps\` 文件夹，桌面根目录保持纯净无散落文件；
  2. 交付包含：`FakeGPS-v2.1-LATEST.apk`、`FakeGPS-v2.1.apk`、`FakeGPS-debug.apk`，并归档了用户的原始黄金基准包 `FakeGPS-ORIGINAL-GOLDEN.apk`。
- **三大核心问题精准修复**：
  1. **开发者选项模拟位置识别修复**：
     - 新增 `XposedStatusHelper.isMockLocationAppSelected(context)`，通过底层 `AppOpsManager.OPSTR_MOCK_LOCATION` (android:mock_location) 及 `MODE_ALLOWED` 精准识别应用是否被系统开发者选项勾选为“选择模拟位置信息应用”；
     - 在“虚拟定位”控制台动态显示“已勾选 / 未勾选”，未勾选时点击一行直接唤醒系统【开发者选项】界面。
  2. **LSPosed 真实生效状态真机校验（杜绝假阳性）**：
     - 彻底摒弃单纯检查是否安装 LSPosed Manager 包名的虚假判断；
     - 通过 `XposedStatusHelper.isLsposedHookReallyActive(context)` 综合双重底层检测：`isModuleActive()`（由 Xposed 框架在 Hook 本应用进程时动态注入返回值）与 `HookStateBridge.isSystemHookAlive()`（系统进程 Hook 心跳）；
     - 只有在 LSPosed 中真正勾选本模块且作用域包含对应系统组件时才显示绿色“已接管系统框架”，否则明确提示“未勾选 · 请在 LSPosed 中勾选并重启手机”，点击直达 LSPosed 模块配置。
  3. **模拟路线划线交互体验优化（彻底消除弹窗遮挡）**：
     - 地图界面新增专属悬浮 **✏️ 划线模式**（Continuous Draw Mode）切换按钮；
     - 点击选点底栏的“✏️ 划线”时，系统将**自动立刻收起遮挡弹窗**，无缝切入划线模式；
     - 在划线模式下，每次在地图上轻触即可**直接连续加点连线**，彻底取消了“每次点选都要弹窗确认”的繁琐遮挡逻辑；
     - 划线模式底部配有超轻量控制胶囊条（显示当前点数、总里程公里数，并提供“撤销”、“清空”、“保存”、“模拟”与“退出”按钮）。

### [2.0.0] - 2026-09-09
- **构建状态**：全新 2.0 正式里程碑版本全量构建，彻底清除所有历史混淆与旧包死循环。
- **Android 配置**：`versionCode = 200`（超越历史上所有构建版本，杜绝系统降级拦截），`versionName = "v2.0"`。
- **清理与重构**：
  1. 彻底清空 `D:\Downloads` 与桌面历史旧包；
  2. 顶部胶囊动态呈现 `当前定位 · v2.0 [构建 2026-09-09 12:xx]`；
  3. 桌面唯一交付：`d:\Desktop\FakeGPS-v2.0-LATEST.apk`。

### [1.3.0] - 2026-09-09
- **构建状态**：审查用户指定基准包 (`bed3e4fd6620ecd53c47b4fbccaf6c2e_544552256908235.apk`)，全量复原并升级完成 (`outputs/app-debug.apk`, `d:\Desktop\FakeGPS-v1.3.apk`)
- **Android 配置**：`versionCode = 103`, `versionName = "v1.3"`
- **流水线执行**：遵循 `AGENTS.md` 规则，构建完成后自动执行 minor +0.1，`package.json` 同步递增至 `1.3.0`。
- **与用户基线 APK 深度比对与对齐成果**：
  1. **底部导航标签与顺序彻底还原**：
     - `地图选点` (Map) -> `虚拟定位` (LocationMock) -> `路线模拟` (RouteSimulation) -> `路线库` (Library)；
     - 彻底消除误更名的“主控状态”和“路线巡航”，重置为用户熟悉的直观经典架构。
  2. **虚拟定位页面 (`LocationMockScreen`) 完整对齐**：
     - 恢复页头副标题：“系统底层虚拟定位与全向漫游”；
     - 恢复“系统防护与保活状态”矩阵及状态指示（LSPosed已接管、Root底层打通、常驻前台保活、电池优化无限制）；
     - 恢复“Root 一键免闪回优化 (关闭 Wi-Fi 硬件探针)”快捷操作；
     - 恢复“🛡️ 彻底防闪回与使用指南”三步指引弹窗（LSPosed勾选系统框架、关闭系统设置Wi-Fi/蓝牙硬件扫描、电池无限制后台保护）。
  3. **路线模拟页面 (`RouteSimulationScreen`) 完整对齐**：
     - 页头恢复为“路线模拟”，副标题“拟真配速与多点巡航漫游”。
  4. **版本号统一动态绑定**：
     - 彻底清除各个界面的硬编码版本号，全量接入 `BuildConfig.VERSION_NAME` 动态响应流水线。

### [1.2.0] - 2026-09-09
- **构建状态**：新项目首个自动化流水线正式构建完成 (`outputs/app-debug.apk`, `d:\Desktop\FakeGPS-v1.2.apk`)
- **Android 配置**：`versionCode = 102`, `versionName = "v1.2"`
- **流水线执行**：遵循 `AGENTS.md` 规则，构建后自动执行 minor +0.1，`package.json` 同步递增至 `1.2.0`。
- **关键交付成果**：
  - 生成 `FakeGPS-v1.2.apk` 并成功部署至 `d:\Desktop\` 与 `d:\Desktop\fake gps\`；
  - 顶部定位状态胶囊实时显示：`当前定位 · v1.2 [构建 2026-09-09 12:01]`；
  - 核心模块全量对齐 `v1.2` 标识（Hook Provider、Manifest、控制台、路线库）。

### [1.1.0] - 2026-09-09
- **构建状态**：基线版本建立完成 (`outputs/app-debug.apk`)
- **Android 配置**：`versionCode = 101`, `versionName = "v1.1"`
- **关键技术特性**：
  - **系统级深度 Hook (`system_server`)**：接管 `LocationManagerService` 与 `LocationProviderManager`，屏蔽 Wi-Fi BSSID 与基站扫描，彻底杜绝闪回与防作弊检测。
  - **连续加点路线规划**：点击铅笔 ✏️ 图标自由连续打点，彻底移除遮挡弹窗，点哪连哪。
  - **沿路智能算路与微步摇杆**：高德/OSM 双底图、道路级精准转弯步数解算与系统级原生悬浮微步摇杆。
  - **生产级健壮性**：有界 LRU 缓存、连接泄漏防御（`finally` 严格 `disconnect()`）、经纬度越界/NaN 异常防御、0 Warning 0 Error 纯净编译。

### [1.0.0] - 2026-09-09
- **Android 配置**：`versionCode = 100`, `versionName = "v1.0"`
- **关键技术特性**：
  - 完成系统级底层 Hook 改造与正统 iOS HIG 毛玻璃视觉重构。
  - 清理所有历史旧构建冲突包。